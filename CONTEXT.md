# DevMate

DevMate 是一个从零实现、零框架依赖的 TypeScript 编程智能体（npm 包 devmate-cli）：用户给出一个编程任务，它自主读写文件、执行命令、反复调用 LLM 直至完成，对标简化版 Claude Code / DeepSeek Harness（npx @deepseek-ai/dsh）。本文件定义该项目的领域术语与规则，术语口径与开发工作区（`.scratch/`，不入库，调研记录见该目录 `coding-agent/research/`）下三份调研报告（openai-compatible-api-spec、open-source-agent-architectures、context-and-error-handling）严格一致，供后续 spec、ADR 与代码统一用语。

## Language

### 主循环与动作

**主循环（Main Loop / Agentic Loop）**:
智能体唯一的大脑循环：把当前上下文发给模型 → 执行模型请求的动作 → 把结果回注 → 如此往复，直到命中某一终止条件。它的最小形态是「查询 + 执行动作」两步，目标对齐 Claude Code 的环境描述（gather context → take action → verify results）。
_Avoid_: 状态机（state machine）、agent runner、编排器

**Harness（智能体壳）**:
模型之外完整的运行层——工具、上下文管理与执行环境；「Agent = Model + Harness」，DevMate 自身就是一个 harness。
_Avoid_: 框架（framework）、运行时（runtime）、插件内核

**Turn（轮次）**:
模型一次完整的请求-响应往返：发一组消息给模型、收到它的回复。一个 Turn 内模型可以不调用工具、调用一个、或并行调用多个。
_Avoid_: 回合、迭代（iteration）、交互

**Step（步）**:
主循环的一次迭代：一次查询加上紧随其后的动作与结果处理。Step 之间以「落盘现场」为界；终止条件的优先级放在查询之前。
_Avoid_: 动作（action）、轮（turn）、迭代（iteration）

**ToolCall（工具调用请求）**:
模型在回复中请求执行的一次动作，一次回复可同时携带多个；执行方必须逐一对每个调用给出结果。
_Avoid_: 函数调用（function call）、动作（action）、指令（command）

**ToolResult（工具执行结果）**:
某次 ToolCall 执行后的产物，包括正常输出、错误、超时与被拒——失败也是一个普通结果；它与发出它的 ToolCall 按调用 ID 严格配对回传。
_Avoid_: 回执（tool receipt）、观察消息（observation）、输出（output）

**动作解析（Action Parsing）**:
把模型输出转成可执行动作的机制：优先走原生工具调用协议，并保留「反引号代码块」解析作为不依赖函数调用能力模型的回退——两条路线的产物是同一类动作。
_Avoid_: 语法解析、Schema 校验、工具调度

**工具面（Tool Surface）**:
暴露给模型执行的工具集合。共识最小面只有两个：一个 shell（含文件读写能力）加一个字符串替换式编辑器；第三梯队（todo/web/子代理）不是正确性必需。
_Avoid_: 插件（plugin）、工具箱、能力集

**编辑格式（Edit Format）**:
模型修改文件所遵循的协议方言——整文件回传、SEARCH/REPLACE 块、简化 udiff 等；没有普适最优的格式，须按模型适配切换。
_Avoid_: patch 格式、补丁

### 终止与保险

**终止条件（Termination Condition）**:
让主循环停下来的条件集合：自然结束、成本上限、步数上限、墙钟上限、连续格式错误、用户中断、压缩不收敛。默认只开启成本上限——最经济的全局保险丝是钱，不是步数。
_Avoid_: 停止条件、退出策略、任务完成判定

**自然结束（Natural End）**:
任务收尾的默认方式：交互式下「本轮不再发起任何工具调用」即告结束。
_Avoid_: 任务完成、显式结束

**提交信号（Submission Marker）**:
评测模式下模型给出的「任务完成」显式信号（输出以预留魔串开头），由环境侧检测到才终止——自然结束只在基准化场景需要附加提交信号。
_Avoid_: 结束魔串、完成信号、提交样式

**保险丝（Fuse）**:
发起下一次查询之前的分项预检查（成本、步数、墙钟三项分开、各自可关）。保险丝必须前置在查询之前，且被计费的失败轮也要补记成本——否则解析失败会成为绕过成本护栏的漏洞。
_Avoid_: 限额（quota）、超时阈值、护栏

**无进展检测（No-Progress Detection）**:
把「模型在兜圈子」代理化为客观可数、与浪费强相关的外显信号：连续格式错误、连续同类工具失败、重复动作、压缩后立刻又满、成本无下降趋势。语义级「它是不是在想正经事」的检测无人做到，不做。
_Avoid_: 死循环检测、意图分析、进展监控

**熔断（Circuit Breaker）**:
某项客观信号连续命中即退出（连同原因回注给用户）的机制：连续格式错误达阈值、压缩防抖、成本超限、连续同类工具失败。一次干净的 Step 清零连续计数；熔断与回注必须成对出现，否则模型会在同一个错误上空转烧钱。
_Avoid_: 重试到死、循环保护、告警

### 会话与状态

**会话（Session）**:
一次运行（run）的 append-only 事件流：系统提示、推理、工具调用与结果、每一次上下文注入都逐事件追加。它是唯一事实来源——resume、fork、search、replay、审计全部同源于它，UI 只是它的一个视图。
_Avoid_: 对话（conversation）、transcript、消息历史、（调试）日志

**对话（Conversation）**:
与模型交互时形成的有序消息序列——它是会话的一个语义视图：会随运行增长、会被压缩改写、可能与磁盘和工作区状态不同步。
_Avoid_: 会话（session）、历史（history）、消息列表

**投影（Projection）**:
由会话推导出的、此刻发给模型的那份上下文：压缩、裁剪、占位等每一层都只作用于投影，append-only 的原始事件流永远不动。resume 时按事件流重建的也是投影。
_Avoid_: 上下文（context）、快照、渲染稿

**轨迹（Trajectory）**:
一次运行全部事件面向审计与回放的可视记录，是会话（Session）的检视视图——会话是唯一事实源，轨迹只是它的视图之一。
_Avoid_: 日志（log）、记录、回放脚本

**会话恢复（Session Resume）**:
在同一会话 ID 的既有事件流上追加新事件继续运行——先进入事件的既成事实不改写；这是唯一合法的「继续」方式，杜绝就地修改历史。
_Avoid_: 续写、重新打开、热启动

**会话分叉（Session Fork）**:
把既有会话的历史复制到新会话 ID，原会话保持不动。
_Avoid_: 分支（branch）、克隆会话

**重放（Replay）**:
按事件流重建会话在任意时刻的投影，用于审查与复现。
_Avoid_: 回滚（roll back）、重演

**写序不变量（Write-Order Invariant）**:
事件落盘的顺序纪律：assistant 消息先落盘 → 才允许执行任何工具 → 工具结果落盘 → 才允许发起下一次请求。它保证任意时刻崩溃只留下一个可用占位符修补的缺口，而不是语义矛盾的历史。
_Avoid_: 批量写、写缓冲、异步记账

**悬空工具调用（Orphaned Tool Call）**:
调用请求已落盘、却缺少对应结果的状态（崩溃或中断造成），其修复方式是补一条「中断占位」结果而非丢弃或重发——把「效果未知」如实告诉模型让它决定是否重新探测。
_Avoid_: 失配调用、半途调用、孤儿调用

**检查点（Checkpoint）**:
写路径动作之前建立的、事后可回滚的工作区指针（核心载体是 git 检查点，其哈希记入对应事件行）。它独立于审批，与 git 自动提交构成「事后补救」路线。
_Avoid_: 撤销（undo）、备份、审核点

**持久规则载体（Persistent Rule Carrier）**:
每次会话开头就注入的项目说明文件（如 CLAUDE.md / AGENTS.md / MEMORY.md）——压缩会丢失早期细节，必须有一份外部持久层承接易失的规则与偏好。
_Avoid_: 记忆文件、系统提示、配置

### 上下文管理

**上下文（Context）**:
模型当前看到的一切输入的总称——它是「上下文管理」一词中上下文的本义，与「上下文窗口」是不同的东西。
_Avoid_: 上下文窗口（context window）、提示词

**两级压缩（Two-Tier Compression）**:
上下文逼近窗口时的固定动作顺序：先清/截旧工具输出（便宜、损失局部化），必要时再对整段对话做摘要（昂贵、有全局损失）。顺序是官方原话规定的，不是我们的发明。「三级瀑布」（生成期截断/组包期裁剪/触顶期摘要）是它在实现侧的分层落地名，属 spec/ADR 范畴。
_Avoid_: 滑动窗口、单级截断、无限摘要

**工具输出截断（Tool-Output Truncation）**:
对单条过大工具输出的无条件裁剪：保留头尾两端、中间替换为「省略了多少」的显式标记并附收窄建议。它是最早触发、成本最低、损失最局部化的第一道闸门，必须在客户端完成。
_Avoid_: 截取、丢弃、裁剪输出

**工具结果裁剪（Tool-Result Pruning）**:
把较早的工具结果按时间序替换为占位符、保留最近若干组的行为，是压缩的第一级主要手段；副作用型工具的结果应予豁免。
_Avoid_: 清理历史、删除输出、归档

**占位符（Placeholder）**:
替换被裁剪结果的内容，明确声明「这里曾有内容被移除」——绝不能装作不存在，否则模型会基于幻觉重建上下文。
_Avoid_: 空字符串、省略号、静默清除

**对话摘要（Conversation Summary）**:
逼近窗口上限时用一次完整采样把整段对话压成结构化摘要（任务概览 / 当前状态 / 重要发现 / 待办 / 需保留上下文），摘要后回到稳定小体积；摘要本身要写入事件流，且摘要提示词应禁止模型调用工具。
_Avoid_: 压缩（compaction）、周期总结、微压缩（microcompaction）
compaction 为协议层术语，本项目领域正术语为本词。

**压缩防抖（Compaction Debounce）**:
若摘要之后上下文立刻又满（压缩不收敛），循环几次后停止自动摘要并显式报错——把「无进展」变成错误，而不是在一条不产生进展的循环里继续烧 API 调用。
_Avoid_: 抖动保护、压缩节流、手动压缩、压缩抖动保护

### 预算、成本与容错

**Token 预算（Token Budget）**:
以「估算 + 校准」闭环管理上下文的余量：本地启发式估算（含消息与工具定义的结构性开销）做请求前预判，服务端真实 usage 做事后校准并滑动更新校正系数；窗口值未知时不硬编码，兜底是「超限报错 → 压缩 → 重试」。
_Avoid_: tokenizer 精确计数、精确预算、usage 统计

**成本护栏（Cost Guard）**:
以钱为单位的全链路防护：请求前检查剩余预算、流式中超阈值即中止并尽力回收已生成部分、响应后用真实 usage 校准累计账本（缓存命中价与未命中价、推理 token 计入输出价）。成本超限同时是熔断与终止条件。
_Avoid_: 费用控制、账单统计、计费器

**错误回注（Error Re-injection）**:
轮次层错误（畸形参数、未知工具、参数校验失败、执行失败、超时、用户拒绝）一律以该次调用的工具结果内容回传给模型（附可枚举的错误类型与给模型的下一步建议），让模型自我修正——失败是一条普通工具结果（ToolResult），绝不因工具失败崩进程；拒绝的拒因也回注，模型才能继续工作。
_Avoid_: 抛异常、报错终止、静默忽略、错误提示

**重试器（Retryer）**:
传输层错误（限流、服务端错误、流式中断）的自动重试：指数退避 + 抖动、尊重 Retry-After、区分可重试与不可重试集合，且只有未产生任何可见增量与工具执行时才允许静默重试。
_Avoid_: 重连逻辑、故障转移、重复请求

**指数退避（Exponential Backoff，Equal Jitter）**:
带上限（cap）和抖动的重试节奏公式；本项目采用 Equal Jitter——单客户端串行场景下不需要打散集群，反而要保证每次重试都有与退避相关的下限等待，Full Jitter 的省概率优势不成立。
_Avoid_: Full Jitter、Decorrelated Jitter、裸指数退避

### 安全与隔离

**工作区监狱（Workspace Jail）**:
允许访问的文件系统边界：启动目录为默认边界、额外目录须显式登记；判定时对符号链接两端都按同一套边界检查（allow 须两端命中、deny 任一命中即拦）、重定向目标按写入检查——它管的是「模型放不放行」，与 OS 级隔离是互补的两层。**边界事实**：门作用于 fs 工具，以及 shell 命令的**重定向目标**；shell 命令的**读侧**（`cat /etc/passwd` 等）越界无门——配合「workspace-write 档全命令零弹窗」可被用于读取 `~/.devmate/config.json` 等（本机信任场景；存储侧以 0700/0600 权限 + 落盘脱敏兜底，凭据形态限制见「机密脱敏」）。
_Avoid_: 沙箱（sandbox）、目录白名单、越界检查

**沙箱（Sandbox）**:
操作系统级隔离（容器/VM、资源限制、网络禁用），权限规则的背后防线：即使提示注入绕过了模型的决策层，沙箱也能挡住命令触达边界外的资源——无人值守时它是唯一真正可靠的防线。
_Avoid_: 工作区监狱、隔离实现、虚拟化

**危险操作审批（Approval）**:
唯一审批面：read-only 档产生的 approval-request（fs 写/编辑 与 ask/deny 级命令——问询兜底）。workspace-write（默认）与 full-access 档不产生任何审批——命令全部直接执行（含 rm -rf 等破坏性；DevMate 无 OS 沙箱强制层，选档即接受风险，前端权限描述承担风险声明）。带备注拒绝时拒因回注给模型使其继续，无备注拒绝则结束本轮。deny 直拒（permission-denied 回注）路径已删除：classify 三判级仍整体消费，但只参与「read-only 档一律 ask」的判定。
_Avoid_: 授权（authorization）、确认弹窗、放行

**许可模式（Permission Mode）**:
dsh 式三档语义（settings 持久化，缺省 workspace-write）：read-only（只读）——唯一产生问询的档：fs 读类与只读命令放行，fs 写/编辑与 ask/deny 级命令按 ask 兜底（弹窗评审）；workspace-write（工作区写入，默认）——fs 全类放行，命令（含 classify ask/deny 级）全部零弹窗直接执行（含破坏性；jail 写边界仍生效）；full-access（全访问）——零问询（一次性风险确认门由前端负责，后端只记 permissionConfirmedAt）。它是交互档位，不是规则来源——jail 边界与 OS 沙箱是它的规则来源。
_Avoid_: 权限级别、安全等级、权限配置文件

**权限规则（Permission Rule）**:
deny → ask → allow 的求值序列，第一个命中的生效，规则的宽窄不影响优先级（宽泛 deny 无法被子集 allow 打洞）；纯工具名 deny 相当于把该工具从模型视野中移除而非运行时拦截。
_Avoid_: ACL（访问控制列表）、策略（policy）、黑名单规则

**只读命令白名单（Read-Only Allowlist）**:
常见只读命令（列目录/查看/查找/统计类及只读 git）的免审批集合；判定前先做命令规则化：剥离包装器、复合命令逐段判定、重定向目标按写入对待、变量赋值前缀不得豁免 deny，且「runner + 内部命令」（npx / docker exec / devbox run / mise exec / direnv exec）必须显式枚举组合——字符串黑名单只是 tripwire，不是安全边界。
_Avoid_: 危险命令黑名单、命令过滤、禁词表

**机密脱敏（Secret Redaction）**:
凭据类敏感内容的掩码处理，两层同一实现（redactSecrets）：① 工具结果回注给模型前（securedRegistry——registry 层唯一咽喉）；② 存储层——kind==='tool' 的结果 content 落盘前（JsonlFileAdapter 默认开）：掩码即最终口径（append 返回值、磁盘、resume/回放一致，模型可见上下文不会出现两次明文）。只覆盖常见凭据形态（AKIA/ghp_/sk-≥24/Bearer/Basic/PEM 块），短 mock 形态不在覆盖内；只作用于 tool 事件（user 消息不脱敏）。
_Avoid_: 日志脱敏、密钥替换

### 执行环境

**常驻 Shell（Persistent Shell）**:
跨多个动作存活的单一长寿命 shell 进程，状态（cwd、环境变量、后台进程）在命令间保持；会话是非交互的（无 vim/less/密码提示、无 GUI）。是「持久会话」与「每次动作独立执行」两派中的默认选择（2026-08-27 用户拍板：常驻会话+哨兵行+异常自动重启；评测/CI 模式另开一次性进程）。两派权衡依据见后续 docs/adr/。
_Avoid_: 一次性子进程（per-action subshell）、独立执行、新 shell

**哨兵行（Sentinel Line）**:
每条命令完成后由 shell 打印的唯一标记行，用于判定该命令输出的边界——通往活进程的管道永远不会有 EOF，不能靠「文件结束」猜测命令何时结束。
_Avoid_: 提示符监测、EOF 判定、超时猜结束

**Shell 重启（Shell Restart）**:
把常驻 shell 及其启动的整棵进程树杀掉、重开一个全新会话（cwd/env/运行中的进程全部消失）的显式能力，用于超时或会话被污染（残留前台进程、PATH 被改）之后的恢复；超时后要先把已捕获的部分输出回注给模型。
_Avoid_: 清理会话、重连、软重启

### 供应商与协议

**供应商（Provider）**:
提供 OpenAI 兼容聊天补全端点的模型平台（OpenAI、DeepSeek、阿里云百炼 DashScope/Qwen、智谱 GLM、Moonshot Kimi 等），Bearer 认证 + messages/tools/SSE 的公共主干完全相同。
_Avoid_: 模型厂、上游、后端（backend）

**Provider Adapter（供应商适配层）**:
把各家协议差异归一为统一口径的一层：采样参数是否可改（过模型白名单，禁改/固定参数先剔除再发）、strict 默认值、parallel 默认、finish_reason 词汇、流式 usage 的放置位置、错误体形状、retry 头有无——差异是事实，统一口径发生在这一层。
_Avoid_: SDK、封装库、下游依赖

**Reasoning Content 策略（推理内容回传策略）**:
推理内容在历史往返中按各家规则分别处置（DeepSeek 必须回传否则 400 / Qwen 默认忽略、回传会改变计费 / GLM 由开关清除 / Kimi 要求原样保留），故按供应商开关三选一：一律剥离、存在即保留、从不发送，并在发送前对同一份内容做归一化——它不是「思考开关」（thinking mode 的开启与强度），是「已产生的推理内容拿历史怎么办」的策略。
_Avoid_: 思维链（CoT）回传、思考策略、推理预算

## Rules

- **术语必须与研究报告口径一致**：任何新概念先在三份报告中找最佳对应词，不许造新词、不许改叫法；报告自身的冲突已由 CTO 于 2026-08-27 裁决（见「术语裁决记录」），后续发现的新冲突仍须列出待裁决，不得单方面统一。
- **会话与对话必须分开说**：resume / fork / replay / 审计只属于会话；压缩、裁剪、截断只属于对话与投影；会话事件流中已落盘的完整事件永不改写，崩溃残留的撕裂尾行可在追加前截断（它从未成为事件），写端新行归一化（补 \n）除外。
- **失败是普通消息**：工具级失败与拒绝一律走错误回注，绝不因工具失败终止进程；回注必须与熔断成对出现。
- **错误分层**：传输层问题（限流/服务端错误/断流）只归重试器，轮次层问题（参数/执行/拒绝）只归错误回注，两层绝不混同重试——同一个畸形输出不能被重发三次、扣三次钱。
- **压缩顺序固定**：先清/截旧工具输出，再摘要；只作用于投影；必须区分「可重算的输出」与「不可重算的状态」，副作用型工具结果永不清理。
- **钱是最有效的保险丝**：成本是默认唯一开启的终止条件；保险丝放在查询之前；被计费的失败轮照记成本。
- **无人值守靠隔离而非审批**：审批只覆盖权限矩阵需问询（ask）项；deny 类直拒（不弹窗回注）与 full-access 零问询都不产生审批流，评测/CI 模式以沙箱与预算兜底。
- **本文件零实现细节**：数值默认值（截断字符数、触发阈值、退避参数）、协议字段、目录结构属于 spec 与 ADR，不进入本文件。

## 术语裁决记录

以下术语分歧已由 CTO 于 2026-08-27 裁决，记录备查：

1. **常驻 Shell vs 每次动作独立执行** → 常驻 Shell + 哨兵行 + restart，评测/CI 另开一次性进程（用户拍板 2026-08-27；哨兵行恰好消除 mini-swe 反持久的第一条理由）。
2. **轨迹（Trajectory）的两种含义** → 会话的检视视图（会话为唯一事实源的架构下视图关系更清晰；mini「轨迹即消息」是其无事件流设计的产物）。
3. **第二级压缩的称呼** → 对话摘要（compaction 仅作协议层对应；microcompaction 为第三方术语，弃用）。
4. **压缩防抖的称呼** → 压缩防抖（「压缩抖动保护」「压缩不收敛熔断」不再作为正术语）。
5. **observation vs ToolResult** → ToolResult（observation 为 mini 专属词，弃用）。
6. **两级压缩 vs 三级瀑布** → 领域术语用「两级压缩」；「三级瀑布」为落地分层名（归 spec/ADR）。
7. **Turn 的中文译名** → 轮次（历史调研原文不改，后续产出统一用词）。
8. **压缩重试上限（报告内部矛盾）** → 研究报告内部矛盾：context-and-error-handling.md §1.4 措辞「重试一次」vs §4.1 E7 与 §8B 的「上限 2 次」→ 以上限 2 次为准（两处依据一致，§1.4 为笔误口径）；具体数值执行归 spec，glossary 只保留定性。
9. **省略标记串（§8 A-1 表「< omitted N chars >」vs §2.3 一手模板「--- N characters elided ---」）** → 以 §2.3 一手模板为准：逐字「--- N characters elided ---」；§8 A-1 表中「< omitted N chars >」属转述变体（说明义），不作为实现字面；实现与测试已按一手模板落地（truncate 模块文件头注释同步指认本记录）。

## 修订说明

2026-08-27 按审查报告 07 修正（第 1-5 项 + 第 8 条裁决）。
2026-08-27 按双轴审查 08 修正：术语裁决记录登记第 9 条（省略标记串冲突）。
2026-08-27 会话事件流条款按 ADR-0004「崩溃残留处理条款」增补：已落盘完整事件永不改写；撕裂尾行可在追加前截断（从未成为事件）；写端新行归一化除外。
