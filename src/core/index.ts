/**
 * # core：Agent 循环核心
 *
 * 汇聚四块职责，对外暴露「跑一次会话」的入口（当前为占位）：
 * - llm/    LLM 客户端与供应商适配层（ADR-0001 / ADR-0002）
 * - context/  上下文管理：投影与两级压缩（ADR-0005）
 * - loop/    主循环：Turn/Step 驱动、终止条件、熔断与重试
 * - tools/   工具面：内置工具集与放行/隔离策略（ADR-0008）
 */
export {};
