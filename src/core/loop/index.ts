/**
 * # loop：主循环（占位）
 *
 * 职责：
 * - Turn/Step 驱动：「查询 -> 执行动作 -> 结果回注」往复，直到命中终止条件。
 * - 终止条件与保险丝：成本、步数、墙钟分项预检查（前置在查询之前），默认只开成本。
 * - 熔断（连续格式错误 / 压缩防抖 / 成本超限）与错误回注成对出现（ADR-0006 / ADR-0013）。
 * - 传输层错误走重试器（指数退避 + Equal Jitter），与轮次层错误回注严格分层。
 */
export {};
