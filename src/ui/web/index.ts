/**
 * # ui/web：原生 Web UI（预留）
 *
 * 零依赖原生 HTML/CSS/ES Modules，作为内置静态资源由与 core 同进程的 local server
 * 提供（ADR-0007）：事件经 HTTP SSE 推流到浏览器，UI 只是会话（Session）的一个视图。
 * 骨架阶段仅占位，预留目录以便 headless/CI 模式永不加载它。
 */
export {};
